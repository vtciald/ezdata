from setuptools import setup, find_packages

setup(
    name="ezdata",
    version="0.1",
    packages=find_packages(),
    # TODO: Add install_requires when things are relatively "complete"
    # TODO: Add extras_require for things needed for dev/testing (pytest)
)